const chico = require('./chico');
const jitters = require('./jitters');
const jasper = require('./jasper');

// combine the three files into an array
const allPets = [ chico, jitters, jasper ];

// export allPets
module.exports = allPets;