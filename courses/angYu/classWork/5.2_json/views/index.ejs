<!DOCTYPE html>
<html>

<head>
   <title>Taco Recipes</title>
   <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
   <h1>🌮 Taco Town 🌮</h1>
   <p>&nbsp;</p>

   <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="beef" name="choice">🥩</button>
      <button type="submit" value="chicken" name="choice">🍗</button>
      <button type="submit" value="fish" name="choice">🐟</button>
   </form>

   <div id="recipeContainer" class="hidden">
      <% if ( locals.tacoType ) { %>
      <h2><%= tacoType.name %> $<%= tacoType.price %></h2>
      <% } else { %>
      <h2>👆 Pick your favorite kind of taco 👆</h2>
      <% } %>

      <h3>Ingredients:</h3>

      <ul id="ingredientsList">
         <li><%= tacoType.ingredients.protein.preparation %> <%= tacoType.ingredients.protein.name %></li>
         <li>with <%= tacoType.ingredients.salsa.spiciness %> <%= tacoType.ingredients.salsa.name %></li>
         <li>&nbsp;</li>
         <li>Topped with:</li>

         <% for (  i =0; i < tacoType.ingredients.toppings.length; i++ )  { %>
            <li><%= tacoType.ingredients.toppings[i].quantity %> <%= tacoType.ingredients.toppings[i].name %> ( <%= tacoType.ingredients.toppings[i].ingredients %> )</li>
         <% } %>
      </ul>

      <p>&nbsp;</p>
   </div>
</body>
</html>
